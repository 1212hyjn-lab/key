package shop.devictoria.api.auth.repository;

import org.springframework.stereotype.Repository;

import shop.devictoria.api.auth.domain.LoginEntity;
import shop.devictoria.api.common.domain.Messenger;

@Repository
public class LoginRepository{
    public Messenger login(LoginEntity loginEntity) {
        return null;
    }
}

